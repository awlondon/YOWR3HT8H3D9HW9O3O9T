# Hooks

Custom hooks for future React/Vue integrations live here. Keeping the folder in
place ensures new UI layers have a canonical location for shared stateful logic
without polluting feature modules.
